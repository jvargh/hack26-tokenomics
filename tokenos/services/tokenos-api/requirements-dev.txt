# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# `requests` is already pulled in transitively (azure-core/msal) when the
# `foundry` extras are installed, but is listed explicitly here because the
# manual live-verification scripts under tests/ (test_full_matrix.py,
# test_workflows_e2e.py, test_workflows_live.py, check_prove_numbers.py) use
# it directly against a running server and must not depend on that transitive
# path.
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
requests>=2.32
playwright>=1.51
